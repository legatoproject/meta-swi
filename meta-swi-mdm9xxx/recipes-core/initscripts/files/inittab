# Note: BusyBox init doesn't support runlevels.  The runlevels field is
# completely ignored by BusyBox init. If you want runlevels, use
# sysvinit.
#
# Format for each entry: <id>:<runlevels>:<action>:<process>
#
# id        == tty to run on, or empty for /dev/console
# runlevels == ignored
# action    == one of sysinit, respawn, askfirst, wait, once,
#              restart, ctrlaltdel, and shutdown.
# process   == program to run

# Run any rc scripts
::sysinit:/etc/init.d/rcS

# Run a shell on the first serial port. Comment this out if you want
# a getty instead
# ttyHSL1::respawn:-/bin/sh

# Uncomment this to run a getty on the first serial port
# ttyHSL0::respawn:/sbin/getty -L ttyHSL0 115200 vt100

# Run a script on shutdown
::shutdown:/etc/init.d/rcK

# Attempt to start getty on UARTx
::respawn:/etc/init.d/run_getty.sh
